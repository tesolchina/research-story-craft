# Student Data

This folder contains local copies of student data for backup and offline access.

## Structure

- `surveys/` - Survey response data
- `progress/` - Task completion progress
- `exports/` - CSV and other export files

## Note

Primary data is stored in the backend database. This local storage serves as:
1. Backup reference
2. Quick access for exports
3. Development/testing data
